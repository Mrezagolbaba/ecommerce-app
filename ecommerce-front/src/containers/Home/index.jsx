import {Header} from "../../components/Header";
import {MenuHeader} from "../../components/MenuHeader";

export const Home = () => {
    return (
        <div>
            <Header/>
            <MenuHeader/>
            Home
        </div>
    );
};