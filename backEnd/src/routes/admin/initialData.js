const {signup,signin,signout} = require( "../../controllers/admin/auth");
const express = require('express');
const router = express.Router();

router.post('/initialData',(res,req) => {

});


module.exports=router;